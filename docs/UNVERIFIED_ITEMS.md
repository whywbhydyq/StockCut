# Unverified / Remaining Items

- `next build` compiled and generated `.next/BUILD_ID`, but did not complete within the sandbox timeout under Node v22.16.0. Run on Node 20.x locally or in Vercel.
- Playwright browser E2E was not re-run successfully in this environment.
- Browser runtime Worker execution requires real-browser verification.
- Browser runtime manual drag interaction requires real-browser verification, although bounds/overlap/rotation logic is unit-tested.
- True angled-cut geometry remains intentionally unsupported; miter/angle fields are planning notes only.
- Machine-ready CNC/router mode and G-code remain intentionally unsupported.
- Chinese route set and affiliate comparison pages remain future content work.
